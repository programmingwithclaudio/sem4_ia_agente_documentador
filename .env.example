# ============================================
# 🔐 Auth Microservice - Environment Variables
# ============================================

# ================== DATABASE ==================
# PostgreSQL Connection (Async for FastAPI)
DATABASE_URL=postgresql+asyncpg://authuser:authpass123@localhost:5432/auth_db

# PostgreSQL Connection (Sync for Alembic migrations)
DATABASE_SYNC_URL=postgresql://authuser:authpass123@localhost:5432/auth_db

# ================== REDIS ==================
# Redis for rate limiting and caching
REDIS_URL=redis://localhost:6379/0

# ================== SECURITY ==================
# Generate a secure secret key with: openssl rand -hex 32
SECRET_KEY=changeme-use-openssl-rand-hex-32-to-generate
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ================== EMAIL ==================
# SMTP Configuration (Example: Gmail)
# For Gmail: Enable "App Passwords" in your Google Account
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
MAIL_FROM=noreply@yourapp.com
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com
MAIL_FROM_NAME=Auth Service
MAIL_TLS=True
MAIL_SSL=False

# ================== FRONTEND ==================
# Your frontend URL (for email links)
FRONTEND_URL=http://localhost:3000

# ================== OAUTH2 (Optional) ==================
# Google OAuth2
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth2
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ================== CORS ==================
# Allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:5173

# ================== RATE LIMITING ==================
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_HOUR=100

# ================== ENVIRONMENT ==================
# development, staging, production
ENVIRONMENT=development

# ================== NOTES ==================
# 1. Never commit this file with real credentials
# 2. Use environment-specific .env files (.env.dev, .env.prod)
# 3. In production, use secrets management (Vault, AWS Secrets, etc)
# 4. For Gmail: https://support.google.com/accounts/answer/185833
# 5. Generate SECRET_KEY: openssl rand -hex 32